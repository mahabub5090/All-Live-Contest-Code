#include <bits/stdc++.h>
using namespace std;
#define ll long long int
#define cn(x) cin >> x
#define foi(i, s, e) for (int i = s; i < e; i++)
#define endl '\n'
#define Endl '\n'
#define yes cout << "Yes\n"
#define no cout << "No\n"

// not solved 

void solve()
{
    int n, m;
    cn(n);
    cn(m);
    string a, b;
    cin >> a >> b;
    
}
int main()
{
    int t;
    t = 1;
    cin >> t;
    // t = 1;
    while (t--)
    {
        solve();
    }
    return 0;
}